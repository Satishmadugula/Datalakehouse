apiVersion: v1
kind: Service
metadata:
  name: {{ include "minio.fullname" . }}
spec:
  selector:
    app: {{ include "minio.fullname" . }}
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
